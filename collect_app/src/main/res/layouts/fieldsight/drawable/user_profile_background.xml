<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The user_profile_background image of Tokyo we will use -->
    <item>
        <bitmap
            android:src="@drawable/sand_city_illustration" />
    </item>

    <!-- Create the gradient so that the this user_profile_background starts of transparent
         and gradually fades to into user_profile_background. This will make the picture look
         like it is gradually fading -->
    <item >
        <shape
            android:shape="rectangle">
            <gradient
                android:centerColor="#b2000000"
                android:angle="270"
                android:startColor="@android:color/transparent"
                android:endColor="@android:color/black">
            </gradient>
        </shape>
    </item>
</layer-list>